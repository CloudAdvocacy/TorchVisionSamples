{
  // Install ESLint and Peacock extensions
  "extensions": [
    "ms-python.python"
  ],

  "settings": {
    "python.pythonPath": "/opt/python/latest/bin/python"
  },

  // Run Bash script in .devcontainer directory
  "postCreateCommand": "source ~/setup/setup.sh"
}